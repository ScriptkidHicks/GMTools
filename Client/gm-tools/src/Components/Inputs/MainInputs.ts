import Autocomplete from "@mui/material/Autocomplete";
import styled from "styled-components";

const SelectorAutoComplete = styled(Autocomplete)({
  minWidth: "300px",
}) as typeof Autocomplete;

export { SelectorAutoComplete };
